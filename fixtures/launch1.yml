env:
  - ENV_VAR_A
  - ENV_VAR_B
  - TRACING_ACCESS_TOKEN
dependencies:
  - workflow-manager
  - dapple
  - dependency-to-skip
aws:
  s3:
    read:
      - common-bucket
      - read-bucket
    write:
      - common-bucket
      - write-bucket
